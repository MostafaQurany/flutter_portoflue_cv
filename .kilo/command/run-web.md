# Run Flutter Web

Launch the project in Chrome for web development testing.

## Command
`flutter run -d chrome --web-renderer canvaskit`

## Notes
Ensure that the responsive layouts are tested at both mobile width (< 600px) and desktop width (> 1000px).